apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mani-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "mani-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mani-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
